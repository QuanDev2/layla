# local cache, not source
data/

__pycache__/
*.pyc
.venv/

# macOS Finder metadata
.DS_Store

# personal secrets — see .env for the one-line-per-KEY=VALUE format
.env

# eval harness scratch — dev-only, not source
dev/eval.md
dev/eval_runs/
